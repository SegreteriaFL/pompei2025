name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate HTML
        uses: actions/html-lint@v1
      - name: Check CSS
        run: npx stylelint "assets/css/*.css"
